# Scribe-Opencode

This project contains both backend and frontend services.

## Backend

The backend is a Python application that serves API endpoints.

## Frontend

The frontend is a React application that provides the user interface.

## Deployment

This project is configured for deployment on Railway.app using the `railway.json` configuration file.